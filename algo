//lcm of two numbers
function findlCM(num1,num2)
{
    
    let min = (num1 > num2) ? num1 : num2;
while (true) {
    if (min % num1 == 0 && min % num2 == 0) {
  
        break;
    }
    min++;
}
    return min
}

let lcm= findlCM(6,10)
console.log(lcm)
